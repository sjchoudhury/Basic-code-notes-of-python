while True:
    line = input('Enter ')
    if line[0] == '#':
        continue
    if line == 'done':
       break
    print(line)

print('Done!')
# it's a break and continue function at specifi code word only it will end
